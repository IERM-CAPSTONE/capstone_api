generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["postgresqlExtensions", "prismaSchemaFolder"]
  engineType      = "binary"
}

datasource db {
  provider     = "postgresql"
  relationMode = "prisma"
}
