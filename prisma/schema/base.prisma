// Prisma Base Configuration
// Generator and Datasource settings

generator client {
  provider   = "prisma-client-js"
  engineType = "binary"
}

datasource db {
  provider     = "postgresql"
  relationMode = "prisma"
}
